# English Learning Storage Rules

## Scope

本文件适用于仓库内后续新增或修改的全部英语学习内容。

## Storage rules

- 生词总库和生词相关资料存入 `vocabulary/`。
- 每周新增生词存入 `vocabulary/weekly/YYYYWww.xlsx`，周编号采用 ISO 格式。
- 固定搭配、习语和短语存入 `phrases/`。
- 值得模仿或复习的完整句子存入 `sentences/`。
- 单词或句子对应的句法结构、时态、语态、搭配和仿写说明存入 `grammar-notes/weekly/YYYYWww.md`。
- 阅读材料的摘要、词汇和理解笔记存入 `reading-notes/`。
- 间隔复习计划、测验和错题记录存入 `review/`。
- PDF、打印版、Anki 导入文件等派生文件存入 `exports/`。
- 可复用的空白表格和笔记格式存入 `templates/`。

## Data rules

- 新内容必须保留来源和添加日期；来源不明时标记为 `unknown`，不要猜测。
- 生词条目至少包含单词、词性、核心释义、例句、来源、添加日期和熟练度。
- 收录单词时不能只保存当前语境义：应按词性列出该词在现代通用英语中的全部常用义项，按常用程度排序，并标明当前语境对应的义项。
- 默认不收录罕见、古旧或高度专业化的义项；若当前来源使用了这类义项，则收录并标注其使用范围。
- 用户要求保存一个单词、短语或句子时，除非用户明确限制范围，同时保存来源句子和针对该句的语法说明。
- 添加生词前检查重复项；同一单词存在多个词性或义项时，在原条目中扩充或明确区分。
- 例句应自然、简洁，并与所记录义项一致。
- 中文释义用于快速理解，英文释义和用法说明用于长期学习。
- 不覆盖历史周文件。修正错误时保留 Git 历史，并在提交说明中写明修正内容。

## File conventions

- 文件名使用小写英文、数字和连字符，不使用空格。
- 日期格式统一为 `YYYY-MM-DD`，周编号统一为 `YYYYWww`。
- Excel 文件优先使用明确的表头、冻结首行和筛选功能；熟练度使用 1–5 的整数。
- 临时文件不得提交；正式导出物才放入 `exports/`。

## Change workflow

1. 确认内容类别与目标目录。
2. 检查是否已有相同词条、短语、句子或笔记。
3. 按既有字段和格式新增内容。
4. 检查拼写、释义、例句、来源和日期。
5. 提交前查看 Git 状态，只提交与本次学习任务相关的改动。
